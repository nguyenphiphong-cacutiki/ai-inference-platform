# Promtail discovers all running containers via the Docker socket, tails their
# logs, attaches useful labels, and pushes them to Loki.
server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml      # remembers how far it has read each log

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  - job_name: docker
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
    relabel_configs:
      # Container name without the leading slash, e.g. "/ollama" -> "ollama".
      - source_labels: ["__meta_docker_container_name"]
        regex: "/(.*)"
        target_label: container
      # The compose service name (handy, stable label for querying in Grafana).
      - source_labels: ["__meta_docker_container_label_com_docker_compose_service"]
        target_label: compose_service
      # Constant label so the whole stream is queryable as {job="docker"}.
      - target_label: job
        replacement: docker
